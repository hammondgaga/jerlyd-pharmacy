# Copy to .env and set strong secrets before running in production.
PORT=3000
JWT_SECRET=change-this-to-a-long-random-string

# Patients can register without this. Pharmacists must send this exact value
# in the "invite code" field when creating a staff account.
PHARMACIST_INVITE_CODE=jerlyd-staff-invite-change-me
